@model Movie

@*<div class="card mb-3">
    <div class="row">
        <div class="col-md-3">
            <img src="/img/@Model.ImageUrl" alt="@Model.Title" class="img-fluid" />
        </div>
        <div class="col-md-9">
            <div class="card-body">
                <h5 class="card-title">@Model.Title</h5>
                <div>
                    <a class="btn btn-primary" asp-controller="Movies" asp-action="Details" asp-route-id="@Model.MovieId">details</a>

                </div>
            </div>
        </div>
    </div>
</div>*@

<div style="display:inline-block">
    <div class="card" style="width: 18rem; ">
        <img src="/img/@Model.ImageUrl" alt="@Model.Title" class="card-img-top" />
        <div class="card-body">
            <div class="text-center">
                <h5 class="card-title">@Model.Title</h5>
            </div>
            <div class="text-center">
                <a class="btn btn-primary" asp-controller="Movies" asp-action="Details" asp-route-id="@Model.MovieId">DETAILS</a>

            </div>
        </div>
    </div>

</div>



